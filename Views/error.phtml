<?php require(__DIR__.'/partials/header.phtml') ?>
<?php require(__DIR__.'/partials/navbar.phtml') ?>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-danger">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>
                        <?= htmlspecialchars($errorMessage ?? 'Error') ?>
                    </h4>
                </div>
                <div class="card-body py-5">
                    <div class="text-center mb-4">
                        <i class="bi bi-shield-lock text-danger" style="font-size: 5rem;"></i>
                    </div>
                    
                    <p class="lead text-center">
                        <?= htmlspecialchars($errorDetails ?? 'An error has occurred.') ?>
                    </p>
                    
                    <div class="d-flex justify-content-center mt-4">
                        <a href="<?= htmlspecialchars($backLink ?? 'index.php') ?>" class="btn btn-outline-secondary">
                            <i class="bi bi-arrow-left me-2"></i>
                            Go Back
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<?php require(__DIR__.'/partials/footer.phtml') ?>